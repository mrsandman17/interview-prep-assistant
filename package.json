{
  "name": "interview-prep-assistant-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo workspace for LeetCode Daily Selector",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "scan:history": "bash scripts/scan-history.sh",
    "scan:staged": "gitleaks protect --staged --verbose",
    "test:all": "npm test --prefix server && npm test --prefix client"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
